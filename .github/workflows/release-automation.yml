name: Release Automation

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  detect-release:
    runs-on: ubuntu-latest
    outputs:
      new_version: ${{ steps.check.outputs.new_version }}
      is_prerelease: ${{ steps.check.outputs.is_prerelease }}
    steps:
      - name: Check for new upstream release
        id: check
        run: |
          # TODO: Query GoogleCloudPlatform/scion releases via gh api
          # TODO: Compare latest release version against current formula version
          # TODO: Set outputs: new_version, is_prerelease
          echo "Not yet implemented"

  build-images:
    needs: detect-release
    if: needs.detect-release.outputs.new_version != ''
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Sync fork from upstream
        run: |
          # TODO: Fetch upstream tags and checkout the new release tag
          echo "Not yet implemented"

      - name: Build and push container images
        run: |
          # TODO: Log in to ghcr.io
          # TODO: Run image-build/scripts/build-images.sh --registry ghcr.io/homebrew-scion --target common --tag <version> --platform all --push
          # TODO: If stable release, retag images as :latest using docker buildx imagetools create
          echo "Not yet implemented"

  update-formula:
    needs: build-images
    if: needs.detect-release.outputs.is_prerelease != 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Download release tarballs and compute checksums
        run: |
          # TODO: Download all 4 platform tarballs from the GitHub release
          # TODO: Compute SHA256 checksums for each tarball
          echo "Not yet implemented"

      - name: Update Homebrew formula
        run: |
          # TODO: Clone homebrew-scion/homebrew-scion using HOMEBREW_TAP_TOKEN
          # TODO: Update version and SHA256 values in Formula/scion.rb
          # TODO: Commit and push the updated formula
          echo "Not yet implemented"
        env:
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
